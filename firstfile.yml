- name: first yaml file
  become: yes
  hosts: all
  tasks:
    - name: debug message
      debug:
          msg: Hello world fuck you !! # not required. The customized message that is printed. If omitted, prints a generic message.
          